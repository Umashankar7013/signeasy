import { ImageWithBasePath } from "./ImageWithBasePath";

export const CrossIcon = () => {
  return (
    <ImageWithBasePath
      src="crossIcon"
      height={20}
      width={20}
      alt=""
      className="cursor-pointer"
    />
  );
};
